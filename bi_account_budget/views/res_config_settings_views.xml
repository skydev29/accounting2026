<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.account.budget</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@id='analytic']" position="inside">
                <setting string="Partial Budget Approve"
                    help="Allow partial budget amounts on budget lines"
                    groups="account.group_account_manager">
                    <field name="partial_budget_approve"/>
                </setting>
                <setting string="Budgetary Positions" groups="account.group_account_manager">
                    <button name="%(bi_account_budget.open_budget_post_form)d"
                        icon="fa-arrow-right" type="action"
                        string="Budgetary Positions" class="btn-link"/>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
